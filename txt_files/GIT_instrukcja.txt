git �-version

git config --global user.name "w3schools-test"
git config --global user.email test@w3schools.com

mkdir myproject        tworzy nowy katalog
cd myproject		zmiana katalogu

git init 
Initialized empty Git repository in /Users/user/myproject/.git/

Ls    	lista plik�w w katalogu

git status       lub    git status --short
tutaj sobie pracujemy i robimy zmiany

git add index.html				dodajemy do fazy staging
git add �-all = git add �A

git commit -m "First release of Hello World!" � komentarz do zmian i zatwierdzenie

git commit -a -m "Updated index.html with a new line" � mo�na pomina� staging

git log
* git�command�-help- Zobacz wszystkie dost�pne opcje dla konkretnego polecenia
* git help --all- Zobacz wszystkie mo�liwe polecenia

git branch hello-world-images � tworzy ga��� hello-world-images
git branch 		- wyswietla ga��zie i pokazuje *w kt�rej jeste�my
git checkout hello-world-images 	- przeskakuje do branch�u
ls  mo�e by� r�ne w r�nych branch�ach

git checkout -b emergency-fix		- tworzy nowy branch (ga���) emergency-fix i od razu do niej przeskakuje

git merge emergency-fix	- ��czy obecn� ga��� z emergency-fix

git branch -d emergency-fix		po zmer�owaniu mo�emy usun� ga��� emergency-fix

czasem mergowanie nie wychodzi: 
CONFLICT (content): Merge conflict in index.html
Automatic merge failed; fix conflicts and then commit the result.

git status � sprawdzamy o co chodzi

przechodzimy do VSCode i tam wy�wietlamy r�nice w branch�ach. Zatwierdzamy r�cznie zmiany

robimy git add plik
git status � m�wi jak wygl�da stan kofliktu
wtedy git commit �m �po��czono ga��zie master i t� drug� po rozwi�zaniu konfliktu�

usuwamy niepotrzebn� ga���:
git branch -d hello-world-images

GITHUB
git remote add origin https://github.com/w3schools-test/hello-world.git	 dodaje zdalne repozytorium z github�a NAZWA REPO w wierszu polece�. Origin to okre�lenie strony w githubie. 

git push --set-upstream origin master(main) � wysyla localne repo do origin

      alternatywnie:
		git branch -M main 
      git push -u origin main

Pracuj�c nad kodem bezpo�rednio w github mo�na commitowa� w origin.
Je�li kto� inny lub my dokonali�my zmian w origin nale�y przed prac� na lokalnym katalogu zaktualizowa� lokalny branch.

git fetch origin � sprawdza aktualizacje origin
git status � podaje zmiany w origin
git log origin/master � pokazuje logi w origin
git diff origin/master � pokazuje roznice
git merge origin/master � scala lokalne repo z origin
git status � 
pull jest kombinacj��fetch i�merge

git pull origin -	 aktualizowa� nasz lokalny Git
git push origin - 	aktualizuje repo w githubie

NOWY BRANCH W GITHUB -> aktualizacja lokalnego repo
git pull   - 	widzimy nowy branch w github
git branch �a	pokazuje wszystkie ga��zie (lokalne i zdalne)
git checkout nazwa nowego branch�u
git pull -	�ci�ga brancha z githuba
git branch � wy�wietla branch�e po aktualizacji

TWORZENIE LOKALNEGO BRANCH�A I WYSY�KA DO GITHUB

git checkout -b update-readme
 teraz wprowadzamy zmiany w pliku, sprawdzamy status i dodajemy staging (git add README.md)
potem commitujemy.
git push origin update-readme -- 	wysy�a nowy branch update-readme do github