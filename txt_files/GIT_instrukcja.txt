git –-version

git config --global user.name "w3schools-test"
git config --global user.email test@w3schools.com

mkdir myproject        tworzy nowy katalog
cd myproject		zmiana katalogu

git init 
Initialized empty Git repository in /Users/user/myproject/.git/

Ls    	lista plików w katalogu

git status       lub    git status --short
tutaj sobie pracujemy i robimy zmiany

git add index.html				dodajemy do fazy staging
git add –-all = git add –A

git commit -m "First release of Hello World!" – komentarz do zmian i zatwierdzenie

git commit -a -m "Updated index.html with a new line" – mo¿na pominaæ staging

git log
* git command -help- Zobacz wszystkie dostêpne opcje dla konkretnego polecenia
* git help --all- Zobacz wszystkie mo¿liwe polecenia

git branch hello-world-images – tworzy ga³¹Ÿ hello-world-images
git branch 		- wyswietla ga³êzie i pokazuje *w której jesteœmy
git checkout hello-world-images 	- przeskakuje do branch’u
ls  mo¿e byæ ró¿ne w ró¿nych branch’ach

git checkout -b emergency-fix		- tworzy nowy branch (ga³¹Ÿ) emergency-fix i od razu do niej przeskakuje

git merge emergency-fix	- ³¹czy obecn¹ ga³¹Ÿ z emergency-fix

git branch -d emergency-fix		po zmer¿owaniu mo¿emy usun¹ ga³¹Ÿ emergency-fix

czasem mergowanie nie wychodzi: 
CONFLICT (content): Merge conflict in index.html
Automatic merge failed; fix conflicts and then commit the result.

git status – sprawdzamy o co chodzi

przechodzimy do VSCode i tam wyœwietlamy ró¿nice w branch’ach. Zatwierdzamy rêcznie zmiany

robimy git add plik
git status – mówi jak wygl¹da stan kofliktu
wtedy git commit –m „po³¹czono ga³êzie master i t¹ drug¹ po rozwi¹zaniu konfliktu”

usuwamy niepotrzebn¹ ga³¹Ÿ:
git branch -d hello-world-images

GITHUB
git remote add origin https://github.com/w3schools-test/hello-world.git	 dodaje zdalne repozytorium z github’a NAZWA REPO w wierszu poleceñ. Origin to okreœlenie strony w githubie. 

git push --set-upstream origin master(main) – wysyla localne repo do origin

      alternatywnie:
		git branch -M main 
      git push -u origin main

Pracuj¹c nad kodem bezpoœrednio w github mo¿na commitowaæ w origin.
Jeœli ktoœ inny lub my dokonaliœmy zmian w origin nale¿y przed prac¹ na lokalnym katalogu zaktualizowaæ lokalny branch.

git fetch origin – sprawdza aktualizacje origin
git status – podaje zmiany w origin
git log origin/master – pokazuje logi w origin
git diff origin/master – pokazuje roznice
git merge origin/master – scala lokalne repo z origin
git status – 
pull jest kombinacj¹ fetch i merge

git pull origin -	 aktualizowaæ nasz lokalny Git
git push origin - 	aktualizuje repo w githubie

NOWY BRANCH W GITHUB -> aktualizacja lokalnego repo
git pull   - 	widzimy nowy branch w github
git branch –a	pokazuje wszystkie ga³êzie (lokalne i zdalne)
git checkout nazwa nowego branch’u
git pull -	œci¹ga brancha z githuba
git branch – wyœwietla branch’e po aktualizacji

TWORZENIE LOKALNEGO BRANCH’A I WYSY£KA DO GITHUB

git checkout -b update-readme
 teraz wprowadzamy zmiany w pliku, sprawdzamy status i dodajemy staging (git add README.md)
potem commitujemy.
git push origin update-readme -- 	wysy³a nowy branch update-readme do github


